class User {
  String? name;
  String? email;
  String? password;
  String? mobileNumber;
  String? imageUrl;

  User(
      {this.name, this.email, this.password, this.mobileNumber, this.imageUrl});
}
